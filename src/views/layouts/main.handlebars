<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <style>
    body { font-family: system-ui, sans-serif; max-width: 900px; margin: 2rem auto; }
    header a { margin-right: 1rem; }
    ul { padding-left: 1.25rem; }
    form { margin: 1rem 0; display: grid; gap: .5rem; max-width: 500px; }
    input, button { padding: .5rem; }
    .row { display: grid; grid-template-columns: 1fr 1fr; gap: .5rem; }
    .card { border: 1px solid #ddd; padding: .75rem; border-radius: .5rem; margin-bottom: .5rem; }
  </style>
</head>
<body>
  <header>
    <a href="/home">Home</a>
    <a href="/realtimeproducts">Real Time Products</a>
  </header>
  {{{ body }}}
</body>
</html>
